# Copy to `.env` and adjust. Restart the dev server after changes.
# CRA only exposes variables prefixed with REACT_APP_.
REACT_APP_RESUME_AUTH_STORAGE_KEY=re_resume_unlocked
# Use quotes if the password contains & or spaces, e.g. REACT_APP_SIGNUP_PASSWORD="your&&pass"
REACT_APP_SIGNUP_PASSWORD=
