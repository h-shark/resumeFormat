# Local dev environment variables (no secrets committed).
# CRA only exposes variables prefixed with `REACT_APP_` into the client bundle.
REACT_APP_RESUME_AUTH_STORAGE_KEY=re_resume_unlocked
# NOTE: The signup password is currently hardcoded in `src/App.js`.
